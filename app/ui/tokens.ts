/** LiteLLM Admin light console. These hexes are rejected and must not reappear in UI. */
export const REJECTED_DARK_GOLD = ["#0c0e12", "#d4a574", "#e0b78a", "#9a958c"] as const;

/** Matches LiteLLM dashboard --background (pure white canvas). */
export const LITE_LLM_BACKGROUND = "oklch(1 0 0)";
